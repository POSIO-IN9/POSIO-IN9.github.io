name: Check Format
on:
  push:
  pull_request:
      branches:
        - main
jobs:
  check-format:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout 🛎️
        uses: actions/checkout@v2
        with:
          persist-credentials: false

      - uses: actions/setup-node@v3.8.1
        with:
          node-version: 18.x
      
      - name: Install 🔧 # Install dependencies
        run: |
          npm install
      
      - name: Check Format
        run: |
          npm run check-format
        continue-on-error: true  # 이 단계가 실패해도 계속 진행
      
      - name: Auto Format 📏 # Auto fix formatting if check-format fails
        run: |
          npm run format
        if: failure()

